base_config: ../config.yaml

experiment:
  tag: minimal_backbone_baseline

model:
  use_hi: false
  use_attention: false

train:
  hi_supervision_weight: 0.0
  hi_rank_weight: 0.0
  hi_variance_weight: 0.0
