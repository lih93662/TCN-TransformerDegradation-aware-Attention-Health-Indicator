base_config: ../config.yaml

experiment:
  tag: ablation_attn_full_degradation

model:
  use_hi: true
  use_attention: true
  attention_use_hi_bias: true
  attention_use_temporal_gate: true
  attention_use_recency_bias: true
